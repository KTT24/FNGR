import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Home Tab")
                .font(.largeTitle)
            Spacer()
        }
        .padding()
    }
}

#Preview {
    HomeView()
}
